<h1>Cart</h1>

<ul id="cart_items">
  <% @cart_items.each do |product| %>
    <li>
      <strong><%= product.name %></strong>
      <em><%= product.price %></em>
      <br />
      <%= link_to "Details", product_path(product) %>
    </li>
  <% end %>
</ul>
